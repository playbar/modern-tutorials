# /usr/src/SDL2/build-scripts/androidbuild.sh:
# find the android update project line, and add --target android-XX to
# it (replace XX with your SDK-installed API).
# See also my http://wiki.libsdl.org/Android

touch test.cpp
/usr/src/SDL2/build-scripts/androidbuild.sh org.wikibooks.opengl test.cpp
rm test.cpp
cp -a /usr/src/SDL2/build/org.wikibooks.opengl/* .
ln -s /usr/src/SDL2_image jni/

cp -a ../common-sdl2/ jni/
cp -a ../tut01_intro-sdl2/triangle.cpp jni/src/test.cpp 
# jni/src/Android.mk: LOCAL_SRC_FILES := ... test.cpp ../common-sdl2/shader_utils.cpp

ndk-build clean
ndk-build -j$(nproc)

ant debug
ant installd

# TODO: implement FS wrapper to access or pre-extract resources (shaders, images...)
# TODO: probably need to add glew wrapper again:
# - relying on GLES2.h won't do for windows, at least not mingw
# - let's expect users will want to try out OpenGL 3 at a point, let's
#   keep the explicit context opening (no Renderer/SDL_Texture...),
#   but stop forcing the "ES" profile
